import React from 'react';

const Inset = (props) => {
  // should allow the user to save info on their overservation
  // maybe add photos
  return (
    <div>
      {/* Use a chakra form */}
    </div>
  )

};

export default Inset;
